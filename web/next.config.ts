import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['joliz.com.br', 'img3.stockfresh.com', 'admin.cnnbrasil.com.br'],
  },
};

export default nextConfig;
